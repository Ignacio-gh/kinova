"""
test_routines.py — Tests de la API de rutinas.

Tests planeados:
    async def test_assign_exercise_ok(...)
    async def test_assign_exercise_duplicate_day_fails(...)
    async def test_get_weekly_routine_grouped_by_day(...)
    async def test_get_today_routine_includes_effective_angles(...)
    async def test_delete_routine_is_soft_delete(...)
    async def test_kine_cannot_assign_to_other_kines_patient(...)
"""

# TODO: tests de rutinas
