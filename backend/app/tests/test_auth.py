"""
test_auth.py — Tests de autenticación.

Tests planeados:
    async def test_register_patient_ok(client)
    async def test_register_patient_duplicate_email_fails(client)
    async def test_register_kinesiologo_ok(client)
    async def test_login_valid_credentials(client, test_patient)
    async def test_login_invalid_password(client, test_patient)
    async def test_login_unknown_email(client)
    async def test_me_with_valid_token(client, auth_headers_patient)
    async def test_me_without_token_fails(client)
"""

# TODO: tests de autenticación
