"""
test_patients.py — Tests de la API de pacientes.

Tests planeados:
    async def test_kine_lists_own_patients(client, auth_headers_kine)
    async def test_kine_cannot_see_other_kines_patients(...)
    async def test_create_patient(client, auth_headers_kine)
    async def test_update_patient_status(client, auth_headers_kine)
    async def test_finalize_patient_deactivates_routines(...)
    async def test_patient_gets_own_dashboard(client, auth_headers_patient)
    async def test_patient_cannot_see_other_patient(client)
"""

# TODO: tests de pacientes
