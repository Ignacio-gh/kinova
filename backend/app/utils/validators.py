"""
validators.py — Validadores reutilizables.

Funciones planeadas:
    def is_valid_email(email: str) -> bool
    def is_valid_matricula(matricula: str) -> bool
    def is_valid_angle_range(angle_min: float, angle_max: float) -> bool
        '''Chequea que min < max y que ambos estén en [0, 180].'''
    def is_valid_day_of_week(day: str) -> bool
        '''Chequea que sea "monday" .. "sunday".'''
"""

# TODO: import re
# TODO: def is_valid_email(email)
# TODO: def is_valid_matricula(matricula)
# TODO: def is_valid_angle_range(min, max)
# TODO: def is_valid_day_of_week(day)
